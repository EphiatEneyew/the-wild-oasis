import Navigation from "../_components/Navigation";

export const metadata = {
  title: "account",
}

export default function Page() {
    return (
    <div>
        <h1>Your Account</h1>
    </div>
    );
}